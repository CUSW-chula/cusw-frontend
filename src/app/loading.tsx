import LoadingClient from '@/components/elements/loading-screen';

export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center font-BaiJamjuree animate-fade-in">
      <LoadingClient />
    </div>
  );
}
